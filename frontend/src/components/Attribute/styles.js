import styled from 'styled-components';

export const Container = styled.div`
  label {
    text-transform: uppercase;
    font-size: 24px;
    padding: 5px;
  }

  label {
    text-transform: uppercase;
    font-size: 24px;
    padding: 5px;
  }

  label:first-letter {
    color: #64666A;
    font-size: 35px;
  }

  input {
    text-transform: uppercase;
    margin-right: 5px;
  }
`;
