import styled from 'styled-components';

export const Container = styled.div`
  .pontos label {
    font-size: 20px;
    text-transform: capitalize;
  }

  .pontos span,
  .pontos label:first-letter {
    font-size: 21px;
    color: #64666A;
  }


  label:first-letter {
    color: #64666A;
    font-size: 35px;
  }

  input {
    text-transform: uppercase;
    margin-right: 5px;
  }

  .pontos label {
    font-size: 20px;
    text-transform: capitalize;
  }

  .pontos span,
  .pontos label:first-letter {
    font-size: 21px;
    color: #64666A;
  }
`;
